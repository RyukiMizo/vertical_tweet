<% provide(:title, "ログイン") %>
<div class="submit-form">
  <div class="left">
    <%= form_for(:session, url: login_path) do |f| %>
      
      <div class="cp_iptxt">
      	<%= f.email_field :email,  placeholder: "E-Mail" %>
      	<i class="fa fa-envelope fa-lg fa-fw" aria-hidden="true"></i>
      </div>
      
      <div class="cp_iptxt">
    	  <%= f.password_field :password,  placeholder: "パスワード(6文字以上)" %>
        <i class="fas fa-key fa-lg fa-fw" aria-hidden="true"></i>
        <%= link_to "（パスワードを忘れた場合）", password_resets_new_path %>
      </div>
   
      <%= f.label :remember_me, class: "checkbox inline" do %>
        <%= f.check_box :remember_me %>
        <span >コンピューターに記憶する</span>
      <% end %>
      
      <%= f.submit "ログイン", class: "btn-gradient-3d sub-bt" %>
    <% end %>
  </div>
</div>   
    <p>新規登録が完了していない場合は<%= link_to "こちら", signup_path %>で登録して下さい！</p>
